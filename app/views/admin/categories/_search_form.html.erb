<%= form_tag admin_categories_path, method: :get do %>
  <p>
    <%= text_field_tag :search, params[:search], placeholder:
      t("search_categories"), class: "form-control txtSearch" %>
    <%= submit_tag yield(:button_text), class: "btn btn-primary" %>
  </p>
<% end %>
